# Collaboration Directive: r01 (Optics/Simulation) ↔ r03 (Hardware/HCI) — CYCLE 5

## Scope
Compute pipeline validation for 5-6ms detection target.

## r01 Responsibilities
- Pattern decoding algorithm benchmark (GPU vs FPGA)
- Binary pattern decode complexity: O(N) vs O(N log N) analysis
- Multi-camera synchronization overhead modeling
- Detection→tracking handoff latency

## r03 Responsibilities  
- GPU platform specifications (RTX 4090 tier = baseline, server GPU options)
- FPGA platform options (Xilinx Kria, Intel Agilex for comparison)
- Real-time OS considerations (Linux kernel bypass, DPC latency)
- End-to-end latency measurement methodology

## Previous Cycle Deliverables (Cycle 4)
- [DELIVERED] Hardware tiers validated ($2K/$10K/$50K)
- [DELIVERED] Camera/projection specs at each tier
- [IDENTIFIED] Compute pipeline as new critical path (3-10ms concern)

## Cycle 5 Task: ACTIVE — CRITICAL PATH
**Investigation:** Can GPU achieve 5ms detection + 1ms pattern decode, or does FPGA become mandatory?

**Sub-task A (r03 compute architecture):**
- GPU detection pipeline: CUDA kernel latency for 1MPx image (1024x1024)
- Memory transfer overhead: Camera→GPU→CPU handoff
- Kernel execution time estimate for binary pattern correlation
- Comparison: Jetson AGX Orin vs RTX 4090 vs server A100 (latency, not throughput)

**Sub-task B (r01 algorithm optimization):**
- Pattern decode: Simple threshold vs matched filter vs FFT correlation
- Target: 1ms decode time per frame per camera
- Parallel decode: Can 4-8 camera streams process simultaneously on single GPU?
- Fallback: FPGA pattern decode if GPU exceeds 2ms

**Deliverable:** Compute pipeline specification document
- GPU path: Expected latency breakdown (capture → decode → output)
- FPGA path: Component costs and expected latency
- Decision matrix: When to choose GPU vs FPGA (density-dependent?)

## Dependency
**BLOCKING:** r02 compute complexity for tracking (arrives Cycle 5)
**BLOCKING:** r05 pattern decode SNR requirements (already delivered)

## Blocker Escalation
[ ] GPU kernel launch overhead exceeds budget  
[ ] PCIe transfer latency dominates processing time  
[ ] Multi-stream synchronization creates serial bottlenecks
[r03] CRITICAL: If GPU path exceeds 7ms total, FPGA becomes mandatory — add 2-week HW procurement

## Notes
Cycle 4 shifted the bottleneck from physics → compute. Cycle 5 determines if we're GPU-first or FPGA-mandatory. This is the highest-priority investigation.
